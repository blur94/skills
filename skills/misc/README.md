# Misc

Skills kept around but rarely reached for.

## Model-invoked

Reachable by the agent on its own, or by name.

- **[ceo-pipeline](./ceo-pipeline/SKILL.md)** — Run the CEO→HR→Researcher→Worker→QA multi-agent pipeline, enforcing contract pre-negotiation, dependency ordering, and decomposition before any implementation. Ships the four agent definitions it spawns in `ceo-pipeline/agents/`.
